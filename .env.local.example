# SMTP-Zugang fuer das Kontaktformular (Brevo).
# Datei nach .env.local kopieren und die Werte eintragen; .env.local bleibt lokal.
BREVO_SMTP_HOST=
BREVO_SMTP_PORT=587
BREVO_SMTP_USER=
BREVO_SMTP_PASSWORD=
